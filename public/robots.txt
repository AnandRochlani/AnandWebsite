# robots.txt for anandrochlani.com
# Canonical host is the APEX domain. The www subdomain is not attached in Vercel
# and fails TLS, so it must never appear in sitemaps, canonicals, or here.

User-agent: *
Allow: /

# Admin and personalised pages emit noindex. They remain crawlable so search
# engines can see that directive and remove any previously indexed copies.

# Never index raw API responses as pages.
Disallow: /api/

Sitemap: https://anandrochlani.com/sitemap.xml
